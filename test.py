temp=[]
out_distance=[-1]*len(graph_vertices)
def distance(start_vertex,six):
	if(len(possible_vertices(start_vertex,graph_vertices,mat))==0 or )
	temp=possible_vertices(start_vertex,graph_vertices,mat)
	for i in temp:
		out_distance[i-1]=six